function C = simple_joint_dtt(i, k, c_k, q)
% i - body id
% k = 1, 2, 3 for x, y and phi
% c_k - value of coordinate to keep all the time
% q - coordinate vector

% % idx_i = body_idx(i);
% % 
% % C(idx_i(k)) = 0;

% C = zeros(1, 3);
% C(k) = 0;

C = 0;